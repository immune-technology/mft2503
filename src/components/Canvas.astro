<canvas id="webgl-canvas" class="absolute top-0 left-0 w-full h-full -z-10"
></canvas>

<style>
  canvas {
    opacity: 0;
    animation: fadeIn 1000ms ease-in-out 500ms forwards;
  }

  @keyframes fadeIn {
    to {
      opacity: 1;
    }
  }
</style>
